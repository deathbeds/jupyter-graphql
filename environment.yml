name: jupyter-graphql-dev

channels:
  - conda-forge
  - defaults

dependencies:
  - aniso8601
  - jupyterlab >=0.34,<0.35
  - nodejs
  - pip
  - promise
  - python >=3.6,<3.7
  - requests
  - rx
  - werkzeug
  - pip:
    - graphene
    - graphql-core
    - graphql-relay
    - gql
