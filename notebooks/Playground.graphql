query {
  	contents(path: "notebooks/GraphQL Editor.ipynb") {
  		... on NotebookContents {
	  		content {
				cells {
          			edges {
                       node {
                         source
                       }
                    }
        		}
			}
		}
	}
}